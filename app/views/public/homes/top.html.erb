  <div class="offset-1">
  	<div>
  		<div class="jumbotron">
  			<div>
  			 <p><strong>
  			  	do you like cat?
          </strong></p>
  			 </div>
  		</div>
  	</div>
  	<h4>新着商品</h4>
  	<div class="row">
      <div class ="d-flex justify-content-between flex-wrap">
        <% @posts.each do |f| %>
          <div class="posts offset-1">
            <%= link_to post_path(f.id) do %>
              <%= image_tag f.get_image %><br>
            <% end %>
            <%= f.title %><br>
           <%= link_to user_path(f.user_id) do %>
            <%= f.user.name %>
          <% end %>
          </div>
        <% end %>
　　　　</div>
      </div>
      <div class ="d-flex justify-content-end">
  	     <%= link_to "すべての商品を見る＞", posts_path %>
  	 </div>
    </div>
</div>